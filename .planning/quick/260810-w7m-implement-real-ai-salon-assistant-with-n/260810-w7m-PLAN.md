---
quick_id: 260810-w7m
status: planned
---

# Real AI Salon Assistant

## Task 1: Backend chat agent and tools

Add validated Hugging Face/OpenAI configuration, a staff-only `/api/chat` endpoint, a bounded tool-call loop, and shared read-only salon tool handlers reused by MCP. Keep provider calls behind a narrow adapter for deterministic tests. Reuse existing service, stylist, booking, and slot services; never self-call `/mcp` over HTTP and never send contact details to the model.

Verify: build the API and run focused chat tests.

## Task 2: Dashboard integration

Regenerate the OpenAPI schema, replace keyword routing with one authenticated typed chat call, and send the full in-memory conversation history from the existing accessible widget while preserving its loading/error behavior.

Verify: build the dashboard.

## Task 3: End-to-end verification

Run backend tests and dashboard build; document required Hugging Face user-secret setup and any provider-dependent live verification not runnable without the user's token.
